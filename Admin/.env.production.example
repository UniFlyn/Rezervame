# Firebase Hosting admin app — API is on Render
NEXT_PUBLIC_API_BASE_URL=https://rezervame.onrender.com/api
NEXT_PUBLIC_WEB_APP_URL=https://rezervame-web.web.app

# Postmark is configured in Admin → Settings → Email (saved to API SystemConfig)
# or via POSTMARK_* env vars on the Render API service (see Backend/.env.example).
